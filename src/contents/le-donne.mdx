---
title: "Le donne"
date: "2023-10-01"
slug: "le-donne"
description: ""
menu_position: 2
---

import DataTb from "../modules/datatb.js"

## Le donne

  <div className="col-sm-12">
    <DataTb
      striped={true}
      dTable="w_persona"
      columns={[
        {
          name: "Nome",
          selector: (item, i) => item.nome,
          sortable: true,
          cell: item => (
            <a href={`../record/?tb=w_persona&id=${item.id}`}>{item.nome}</a>
          ),
        },
        {
          name: "Famiglia di appartenenza",
          selector: (item, i) => item.famiglia_gens,
          sortable: true,
        }, 
        {
          name: "Provenienza",
          selector: (item, i) => item.provenienza,
          sortable: true,
        }, 
        {
          name: "Vissuta tra",
          selector: (item, i) => {
            const birthYear = item.anno_nascita !== null ? (item.anno_nascita < 0 ? `${-item.anno_nascita} a.C.` : `${item.anno_nascita} d.C.`) : '?';
            const deathYear = item.anno_morte !== null ? (item.anno_morte < 0 ? `${-item.anno_morte} a.C.` : `${item.anno_morte} d.C.`) : '?';
            return `${birthYear} — ${deathYear}`;
          },
          sortable: true,
        }
      ]}
    />
  </div>
